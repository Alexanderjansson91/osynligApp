import React from "react";
import {
    View,
    Text,
    StyleSheet
} from "react-native";

const NewTask = (props) => {
    return (
        <View >
           <Text>New</Text> 
        </View>
    )}
    export default NewTask;

    const styles = StyleSheet.create({
container:{
    flex:1,
    alignItems:'center',
    justifyContent:'center'
    }
});